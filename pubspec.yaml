name: greengrocer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  add_to_cart_animation: ^1.0.1
  animated_text_kit: ^4.2.2
  badges: ^2.0.3
  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  flutter:
    sdk: flutter
  flutter_secure_storage: ^6.0.0
  fluttertoast: ^8.0.9
  freezed_annotation: ^2.1.0
  get: ^4.6.5
  intl: ^0.17.0
  mask_text_input_formatter: ^2.3.0
  qr_flutter: ^4.0.0
  shimmer: ^2.0.0

dev_dependencies:
  build_runner: ^2.2.0
  flutter_launcher_icons: ^0.10.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.1.0+1

flutter:

  uses-material-design: true
  assets:
    - assets/fruits/
    - assets/app_images/
  #   - images/a_dot_ham.jpeg
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_images/app_icon.png"
  remove_alpha_ios: true

scripts:
  freezedbuild: flutter pub run build_runner build --delete-conflicting-outputs
